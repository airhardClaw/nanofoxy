gates:
- fast_gate
- repo_gate
- harness_gate
commands:
- uv run pytest -q
- uv run ruff check src tests scripts
- cd frontend/terminal && ([ -x ./node_modules/.bin/tsc ] || npm ci --no-audit --no-fund)
  && ./node_modules/.bin/tsc --noEmit
require_tests_before_merge: true
